# node-side state that must never be committed
.sign-cache.json
data/users/*/
pool/
*.db
*.db-*

# archived React source only (no build tooling at the root any more)
archive/react/node_modules/
archive/react/dist/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*.local
.env
*.tsbuildinfo
